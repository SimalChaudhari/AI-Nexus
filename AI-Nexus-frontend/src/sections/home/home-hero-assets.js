import heroMainImage from 'src/assets/home/hero.png';

// ----------------------------------------------------------------------

/** Local hero artwork from `src/assets/home/` (bundled by Vite). */
export const HOME_HERO_ASSETS = {
  main: heroMainImage,
};
