'use client';

import { IntlAppLayout } from 'src/sections/international/intl-app-topbar';

export default function AiFluencyLayout({ children }) {
  return <IntlAppLayout>{children}</IntlAppLayout>;
}
