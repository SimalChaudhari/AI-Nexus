'use client';

import { PathwayPlannerView } from 'src/sections/international/pathway/pathway-planner-view';

/** Standalone role planner — same UI as before (also shown in dashboard Roles section). */
export default function AiFluencyRolesPage() {
  return <PathwayPlannerView />;
}
